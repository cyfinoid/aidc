<!-- aidc-managed -->
---
description: Shared CORE_LOGICS guidance
alwaysApply: true
---

Read `/opt/CORE_LOGICS/patternlist.md` before starting work.

Reusable guidance belongs in `/opt/CORE_LOGICS` on the current project branch. Keep repo changes inside `/workspace` unless the task explicitly targets `/opt/CORE_LOGICS`.
